{
  "name": "@senn/core",
  "version": "0.0.0",
  "private": true,
  "description": "SENN Core runtime — rooms, P2P transport, message routing, add-on host.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "echo \"no tests yet\""
  },
  "dependencies": {
    "@senn/protocol": "workspace:*",
    "@senn/storage": "workspace:*"
  }
}
