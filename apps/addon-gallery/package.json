{
  "name": "@senn/addon-gallery",
  "version": "0.0.0",
  "private": true,
  "description": "SENN add-on gallery — discovery, trust display, install hand-off (ADR-0016).",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "e2e": "playwright test --project=chromium --project=firefox",
    "e2e:all": "playwright test",
    "e2e:webkit": "playwright test --project=webkit",
    "e2e:ui": "playwright test --ui"
  },
  "dependencies": {
    "@senn/manifest": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "vite": "^8.0.10"
  }
}
